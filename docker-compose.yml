services:
  # Service for running the pipeline scripts
  pipeline:
    build: .
    env_file: .env
    environment:
      - PYTHONPATH=/app
    volumes:
      - .:/app
    # Default command - run the interactive CLI
    command: bash scripts/cli.sh
    stdin_open: true
    tty: true